Use the screen description and candidates in `/app/login.json` to select the next action that submits the completed login form.

No GUI interaction is required. Save a JSON object containing the selected `element_id` in the `selected` field to `/app/prediction.json`.
